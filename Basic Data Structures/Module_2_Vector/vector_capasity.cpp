#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> v;
    cout << v.capacity();
    v.push_back(10);
        cout << v.capacity();
    v.push_back(10);
        cout << v.capacity();
    return 0;
}